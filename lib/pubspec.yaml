name: bestchat_welcome
description: A simple welcome screen for Bestchat app with dark/light mode choice.
publish_to: 'none' # this package is not published
version: 0.0.1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
